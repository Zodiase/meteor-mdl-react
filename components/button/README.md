MaterialButton
==============

All available options
---------------------
```HTML
<MaterialButton
    className={extraClassnames}
    disabled={isDisabled}
    colored={isColored}
    raised={isRaised}
    primary={isPrimary}
    accent={isAccent}
    fab={isFab}
    icon={isIcon}
>
Content
</MaterialButton>
```
